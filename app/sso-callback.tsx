/**
 * Expo Go opens this route after the browser completes an OAuth flow.
 * Clerk's `useSSO` hook reads the callback URL and activates the session.
 */
export default function SsoCallbackScreen() {
  return null;
}
